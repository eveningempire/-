# 单测点规则报告（自动选择 + 候选Top-K + 组合试探）

## 测点：高速转速

- **最佳单指标**：`slope` —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。

- **候选单规则（Top-K）及增益**：
  - `slope`: gain=0.5343 —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。
  - `level`: gain=0.3094 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
- **组合建议**：策略=`and`，指标=`slope`, `level`，**gain=0.5503**
  - 解释：需多指标共同异常才高分，**更稳健**（适合误报成本高）。
- **所有单规则参数**：
  - `level`: median=7000, MAD=0.020027, tail=two
  - `diff`: median=0.020027, MAD=0.020027, tail=upper
  - `rollstd`: median=0.0176985, MAD=0.00369786, tail=upper
  - `lowvar`: median=0.0176985, MAD=0.00369786, tail=lower
  - `slope`: median=0.000156461, MAD=0.000156461, tail=upper

## 测点：低速转速

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=0.002918, MAD=2.51621, tail=two
  - `diff`: median=0.0604845, MAD=0.0535105, tail=upper
  - `rollstd`: median=2.42555, MAD=1.45154, tail=upper
  - `lowvar`: median=2.42555, MAD=1.45154, tail=lower
  - `slope`: median=0.04607, MAD=0.0297638, tail=upper

## 测点：低速位置

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=96.9303, MAD=6.93993, tail=two
  - `diff`: median=0.160847, MAD=0.159646, tail=upper
  - `rollstd`: median=5.9837, MAD=4.32057, tail=upper
  - `lowvar`: median=5.9837, MAD=4.32057, tail=lower
  - `slope`: median=0.131108, MAD=0.0898734, tail=upper

## 测点： +12V遥测

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=3.35938, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0, MAD=0, tail=upper
  - `lowvar`: median=0, MAD=0, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点： +5V遥测

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`or`，指标=`level`, `rollstd`，**gain=0.0000**
  - 解释：任一指标显著异常即高分，**更敏感**（适合漏检成本高）。
- **所有单规则参数**：
  - `level`: median=3.67188, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0.00486349, MAD=0.00141086, tail=upper
  - `lowvar`: median=0.00486349, MAD=0.00141086, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点： +45V遥测

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=4.14062, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0, MAD=0, tail=upper
  - `lowvar`: median=0, MAD=0, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点： 100V遥测

- **最佳单指标**：`slope` —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。

- **候选单规则（Top-K）及增益**：
  - `slope`: gain=1.0000 —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。
  - `rollstd`: gain=0.1047 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
- **组合建议**：策略=`single`，指标=`slope`，**gain=1.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=4.25781, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0.00345263, MAD=0.00345263, tail=upper
  - `lowvar`: median=0.00345263, MAD=0.00345263, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：高速电机电压

- **最佳单指标**：`rollstd` —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。

- **候选单规则（Top-K）及增益**：
  - `rollstd`: gain=0.5416 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
- **组合建议**：策略=`single`，指标=`rollstd`，**gain=0.5416**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=3.35938, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0.013633, MAD=0.00527085, tail=upper
  - `lowvar`: median=0.013633, MAD=0.00527085, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：高速固紧端轴温

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`and`，指标=`level`, `rollstd`，**gain=1.0000**
  - 解释：需多指标共同异常才高分，**更稳健**（适合误报成本高）。
- **所有单规则参数**：
  - `level`: median=2.73438, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0, MAD=0, tail=upper
  - `lowvar`: median=0, MAD=0, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：高速滑动端轴温

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`and`，指标=`level`, `rollstd`，**gain=1.0000**
  - 解释：需多指标共同异常才高分，**更稳健**（适合误报成本高）。
- **所有单规则参数**：
  - `level`: median=2.73438, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0, MAD=0, tail=upper
  - `lowvar`: median=0, MAD=0, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：低速壳温

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=2.46094, MAD=0.039062, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0.00486349, MAD=0.00486349, tail=upper
  - `lowvar`: median=0.00486349, MAD=0.00486349, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：高速电机电流

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.3212 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `slope`: gain=0.1569 —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
- **组合建议**：策略=`and`，指标=`level`, `slope`，**gain=0.3533**
  - 解释：需多指标共同异常才高分，**更稳健**（适合误报成本高）。
- **所有单规则参数**：
  - `level`: median=0.862334, MAD=0.031035, tail=two
  - `diff`: median=0.017735, MAD=0.0133, tail=upper
  - `rollstd`: median=0.0188129, MAD=0.00341421, tail=upper
  - `lowvar`: median=0.0188129, MAD=0.00341421, tail=lower
  - `slope`: median=0.000207828, MAD=0.000138547, tail=upper

## 测点：低速A相电流

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`level`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=0.054688, MAD=0.027344, tail=two
  - `diff`: median=0.013672, MAD=0.013672, tail=upper
  - `rollstd`: median=0.0376185, MAD=0.0151321, tail=upper
  - `lowvar`: median=0.0376185, MAD=0.0151321, tail=lower
  - `slope`: median=0.000320438, MAD=0.000186998, tail=upper

## 测点：低速C相电流

- **最佳单指标**：`diff` —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。

- **候选单规则（Top-K）及增益**：
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`single`，指标=`diff`，**gain=0.0000**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=-0.013672, MAD=0.054688, tail=two
  - `diff`: median=0.013672, MAD=0.013672, tail=upper
  - `rollstd`: median=0.0569001, MAD=0.0267111, tail=upper
  - `lowvar`: median=0.0569001, MAD=0.0267111, tail=lower
  - `slope`: median=0.000747688, MAD=0.000320445, tail=upper

## 测点：定角锁定精度

- **最佳单指标**：`level` —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。

- **候选单规则（Top-K）及增益**：
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
  - `rollstd`: gain=0.0000 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
- **组合建议**：策略=`mean`，指标=`level`, `diff`，**gain=0.0040**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=2.42155, MAD=2.37228, tail=two
  - `diff`: median=0.05802, MAD=0.05578, tail=upper
  - `rollstd`: median=1.40335, MAD=0.768614, tail=upper
  - `lowvar`: median=1.40335, MAD=0.768614, tail=lower
  - `slope`: median=0.016674, MAD=0.0148742, tail=upper

## 测点：高速转速控制精度

- **最佳单指标**：`slope` —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。

- **候选单规则（Top-K）及增益**：
  - `slope`: gain=0.9824 —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。
  - `level`: gain=0.0000 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
  - `diff`: gain=0.0000 —— 瞬时波动：相邻样本差分的幅度（取绝对值），上尾检测。适合捕捉抖动、冲击。
- **组合建议**：策略=`single`，指标=`slope`，**gain=0.9824**
  - 解释：综合多指标强弱，兼顾敏感与稳健；加权可突出关键指标。
- **所有单规则参数**：
  - `level`: median=6e-05, MAD=0, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=0, MAD=0, tail=upper
  - `lowvar`: median=0, MAD=0, tail=lower
  - `slope`: median=0, MAD=0, tail=upper

## 测点：高速转速控制稳定度

- **最佳单指标**：`slope` —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。

- **候选单规则（Top-K）及增益**：
  - `slope`: gain=0.5503 —— 趋势/漂移：短窗均值的差分（绝对值），上尾检测。适合缓慢漂移/趋势走偏。
  - `rollstd`: gain=0.5154 —— 局部方差：短窗滚动标准差，上尾检测。适合发现波动变大、控制不稳。
  - `level`: gain=0.3255 —— 幅值偏离：测点值相对健康中位数的偏移程度，双尾检测。适合有稳定工作点的量。
- **组合建议**：策略=`and`，指标=`slope`, `level`，**gain=0.7302**
  - 解释：需多指标共同异常才高分，**更稳健**（适合误报成本高）。
- **所有单规则参数**：
  - `level`: median=0.0011, MAD=5e-05, tail=two
  - `diff`: median=0, MAD=0, tail=upper
  - `rollstd`: median=3.85436e-06, MAD=3.85436e-06, tail=upper
  - `lowvar`: median=3.85436e-06, MAD=3.85436e-06, tail=lower
  - `slope`: median=7.8125e-08, MAD=7.8125e-08, tail=upper
