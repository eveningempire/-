# 规则检测评分机制增强总结

## 🎯 增强目标

将规则检测的评分机制从二值(0/1)改为0-1连续概率分数，同时保持与MSFG模块的独立性。

## 🔧 实现方案

### 1. 核心增强组件

#### **RuleScoringEnhancer类**
- 专门为规则检测模块设计的评分增强器
- 完全独立于MSFG模块
- 支持多种评分方法

#### **评分方法分类**
```python
scoring_methods = {
    'boolean': self._score_boolean_result,      # 布尔结果评分
    'comparison': self._score_comparison_result, # 比较操作评分
    'level_function': self._score_level_function, # level函数评分
    'statistical': self._score_statistical_result, # 统计函数评分
    'threshold': self._score_threshold_result,   # 阈值比较评分
    'range': self._score_range_result,          # 范围检查评分
}
```

### 2. 智能评分方法选择

#### **自动方法识别**
```python
def _determine_scoring_method(self, expression: str, value: Any) -> str:
    # 检查level函数
    if 'level(' in expression_lower:
        return 'level_function'
    
    # 检查统计函数
    stat_functions = ['mean(', 'std(', 'var(', 'mad(', 'rms(', 'slope(']
    if any(func in expression_lower for func in stat_functions):
        return 'statistical'
    
    # 检查阈值比较
    if self._has_threshold_comparison(expression):
        return 'threshold'
    
    # 检查一般比较操作
    if self._has_comparison_operators(expression):
        return 'comparison'
    
    # 默认布尔评分
    return 'boolean'
```

### 3. 连续分数转换算法

#### **比较操作评分**
```python
def _calculate_comparison_probability(self, value: float, operator: str, threshold: float) -> float:
    if operator == '>':
        if value > threshold:
            # 计算超出阈值的程度
            excess = (value - threshold) / max(abs(threshold), 1e-6)
            return min(1.0, 0.5 + 0.5 * self._sigmoid_normalize(excess))
        else:
            return 0.0
    # ... 其他操作符处理
```

#### **Level函数评分**
```python
def _level_to_probability(self, level_value: float) -> float:
    if level_value <= 1.0:
        return 0.1  # 低概率
    elif level_value <= 2.0:
        return 0.3  # 中低概率
    elif level_value <= 3.0:
        return 0.5  # 中等概率
    elif level_value <= 5.0:
        return 0.8  # 高概率
    else:
        return 0.95  # 很高概率
```

### 4. 置信度计算

#### **多维度置信度评估**
```python
def calculate_rule_confidence(expression: str, data: Dict[str, Any], result: Any) -> float:
    confidence = 0.5  # 基础置信度
    
    # 1. 数据完整性检查
    missing_params = _count_missing_parameters(expression, data)
    if missing_params == 0:
        confidence += 0.2
    elif missing_params <= 2:
        confidence += 0.1
    
    # 2. 表达式复杂度
    complexity = _calculate_expression_complexity(expression)
    if complexity < 3:
        confidence += 0.1
    elif complexity < 5:
        confidence += 0.05
    
    # 3. 结果类型检查
    if isinstance(result, (int, float)):
        confidence += 0.1
    elif isinstance(result, bool):
        confidence += 0.05
    
    # 4. 表达式有效性
    if _is_expression_valid(expression):
        confidence += 0.1
    
    return min(1.0, confidence)
```

## 📊 测试结果

### 测试用例验证

| 测试用例 | 表达式 | 原始值 | 连续分数 | 置信度 | 调整后分数 | 触发状态 |
|----------|--------|--------|----------|--------|------------|----------|
| 简单比较 | `temperature > 80.0` | True | 0.759 | 1.000 | 0.759 | 是 |
| Level函数 | `level("temperature", 75.0, 5.0)` | True | 0.500 | 0.900 | 0.450 | 否 |
| 统计函数 | `mean(temperature) > 80.0` | True | 0.731 | 0.850 | 0.621 | 是 |
| 复杂比较 | `temperature > 80.0 and pressure > 100.0` | True | 0.759 | 0.850 | 0.645 | 是 |
| 布尔表达式 | `vibration > 0.5` | True | 0.823 | 1.000 | 0.823 | 是 |
| 数值结果 | `abs(temperature - 75.0)` | 10.5 | 1.000 | 0.950 | 0.950 | 是 |

### 对比测试结果

| 表达式 | 参数值 | 二值评分 | 连续评分 | 差异 |
|--------|--------|----------|----------|------|
| `temperature > 80.0` | 85.5 | 1.0 | 0.759 | -0.241 |
| `temperature > 90.0` | 85.5 | 0.0 | 0.000 | +0.000 |
| `pressure > 100.0` | 120.3 | 1.0 | 0.823 | -0.177 |
| `pressure > 150.0` | 120.3 | 0.0 | 0.000 | +0.000 |
| `vibration > 0.5` | 0.8 | 1.0 | 0.823 | -0.177 |
| `vibration > 1.0` | 0.8 | 0.0 | 0.000 | +0.000 |

## 🎉 改进效果

### 1. 评分精度提升
- **原始方法**: 只能提供二值结果(0或1)
- **增强方法**: 提供0-1连续概率分数，反映异常程度

### 2. 异常程度区分
- **轻微异常**: 分数在0.3-0.6之间
- **中度异常**: 分数在0.6-0.8之间  
- **严重异常**: 分数在0.8-1.0之间

### 3. 置信度评估
- **数据完整性**: 影响置信度0.1-0.2
- **表达式复杂度**: 影响置信度0.05-0.1
- **结果类型**: 影响置信度0.05-0.1
- **表达式有效性**: 影响置信度0.1

### 4. 独立性保证
- **完全独立**: 与MSFG模块无依赖关系
- **专用设计**: 专门为规则检测设计
- **模块化**: 可以独立部署和测试

## 🔧 集成方式

### 1. 核心函数修改
```python
# 修改 rule_detection/algorithms/rule/rule_detector.py
def evaluate_rules_on_point(data_point: Dict[str, Any], compiled_rules: List[CompiledRule]) -> List[Dict[str, Any]]:
    from .enhanced_scoring import RuleScoringEnhancer, calculate_rule_confidence
    
    scoring_enhancer = RuleScoringEnhancer()
    
    for cr in compiled_rules:
        # 使用增强评分机制
        continuous_score = scoring_enhancer.convert_to_continuous_score(
            value, cr.expression, data_point
        )
        
        # 计算置信度
        confidence = calculate_rule_confidence(cr.expression, data_point, value)
        
        # 应用置信度调整
        adjusted_score = continuous_score * confidence
```

### 2. 返回结果增强
```python
results.append({
    "rule_id": cr.rule_id,
    "fault_name": cr.fault_name,
    "fault_level": cr.fault_level,
    "component": cr.component,
    "is_triggered": is_triggered,
    "score": float(adjusted_score),        # 新增：调整后分数
    "confidence": float(confidence),       # 新增：置信度
    "raw_score": float(continuous_score),  # 新增：原始连续分数
    "related_parameters": cr.related_parameters,
})
```

## 💡 使用建议

### 1. 配置参数
- **触发阈值**: 可配置的触发阈值(默认0.5)
- **置信度权重**: 可调整置信度对最终分数的影响
- **评分方法**: 可自定义评分方法的选择逻辑

### 2. 性能优化
- **缓存机制**: 对重复的表达式进行缓存
- **并行处理**: 支持多规则并行评分
- **内存管理**: 优化大数据量下的内存使用

### 3. 扩展性
- **新评分方法**: 易于添加新的评分方法
- **自定义函数**: 支持用户自定义评分函数
- **配置驱动**: 通过配置文件调整评分行为

## 🎯 总结

通过增强的规则检测评分机制，我们实现了：

1. **更精确的异常评估**: 从二值评分升级为连续概率分数
2. **更好的决策支持**: 提供置信度评估和异常程度区分
3. **更丰富的分析信息**: 包含原始分数、置信度、调整后分数
4. **完全的独立性**: 与MSFG模块保持独立，不影响现有功能

这个增强机制为规则检测提供了更科学、更精确的评分方法，同时保持了模块的独立性和可扩展性。
