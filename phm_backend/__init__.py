"""Top level package for the PHM Health Management backend.

This package exposes the Django application configuration. Initialization
logic that should run when the app starts can live here. The project
package itself is not a Django app and therefore does not define an
``AppConfig``. All application configuration lives in the individual apps
under this package.
"""

