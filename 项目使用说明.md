# 重复使用运载器健康管理平台使用说明

压缩包已包含 `db.sqlite3`、`media/datasets/` 下的示例 CSV、MATLAB 示例脚本和前端构建文件；未包含 `.venv`、`node_modules` 和 Git 历史。

## 启动

1. 安装 Python 3.10+、Node.js 18+；MATLAB 实时采集另需 MATLAB 和 Redis。
2. 根目录创建并激活环境：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pip install django djangorestframework channels daphne whitenoise django-cors-headers pandas numpy torch
cd frontend; npm install; cd ..
```

压缩包没有携带 `.venv`。如果看到“找不到 `..venv\Scripts\python.exe`”，请确认命令中的路径是 `.\.venv`（两个点后面有一个反斜杠），并先执行上面的环境创建命令。环境激活后也可以直接使用：

```powershell
python manage.py migrate
python manage.py runserver 127.0.0.1:8000
```

3. 双击 `start_cmg_platform.bat`，访问 `http://localhost:5173`。

## 登录

数据库已有管理员账号 `ADMIN`。如需创建管理员，运行：

```powershell
.\.venv\Scripts\python.exe create_superuser.py
```

## MATLAB 实时采集

在“实时状态监测 → 选择数据源 → MATLAB 实时采集”中创建会话，保存会话 ID、上报地址和 Token。

MATLAB 执行：

```matlab
addpath('项目目录/integrations/matlab');
addpath('项目目录/integrations/matlab/examples');
demo_send_telemetry('http://127.0.0.1:8000/api/v1/phm/telemetry/sessions/<会话ID>/ingest/', '<Token>');
```

示例文件为 `integrations/matlab/examples/demo_send_telemetry.m`，会发送 20 条 `time`、`pressure`、`temperature` 数据。`time` 必须严格递增，字段集合必须一致，不能使用 NaN 或 Inf。

## CSV 与故障诊断

在“数据管理”上传 CSV，进入“故障诊断分析”，选择数据集并运行 Hier14。页面显示类别概率、滑动窗口趋势和窗口明细。若只有一个窗口，请增加 CSV 样本数。

项目数据库已登记示例数据。若页面没有示例数据，先执行 `python manage.py migrate` 并使用管理员登录；也可重新上传 `media/datasets/2026/09/12/` 下的 CSV 文件。

## 状态检查

访问 `http://localhost:8000/api/v1/fault-diagnosis/status/`，正常时 `available` 为 `true`。修改前端后使用 `Ctrl+F5` 刷新。

## 常见问题

- `No data`：尚未创建会话或 MATLAB 尚未上报。
- Token 无效：使用创建会话时返回的新 Token。
- 时间必须递增：仿真归零后新建会话。
- Hier14 未启用：重启 Django，并查看状态接口的 `error`。
- 远程 MATLAB：将 `localhost` 改为服务器 IP，并配置 `ALLOWED_HOSTS`。

停止服务可运行 `stop_cmg_platform.bat`。
