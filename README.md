# 航天器 PHM 平台（项目骨架）

本目录以 `cmg_migration_package` 的 Django + 前端分层为基础，面向可重复使用航天器健康管理（PHM）。当前只完成工程框架和接口契约，算法、真实遥测与仿真数据暂不纳入。

目录对应 CMG 的数据管理、健康管理、规则检测、多信号流图、寿命预测、智能感知和用户权限模块；新增 `phm/` 作为领域编排层。

算法接入顺序建议为：测点/数据字典 → 结构模型 → 故障诊断 → 健康指数（HI）→ RUL → 维修决策。

